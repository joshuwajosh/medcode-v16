# FILE_ANALYSIS.md — Suspected Dead Files

Generated by deep analysis of 24 files at project root. For each: purpose, cross-references, imports from main codebase, and deletion verdict.

---

## Legend

- **Referenced by code** = another `.py` file imports or `exec`s it (not just mentioned in `.md` docs)
- **SAFE TO DELETE** = no code dependency, standalone utility/debug/test/data artifact
- **MUST KEEP** = imported by production code, used by CI/runner, or serves as data dependency

---

## 1. debug_cpt.py

| Field | Value |
|---|---|
| Purpose | Standalone script to test CPT semantic search and BFS traversal on a colonoscopy note |
| Imports from main codebase | `core.models`, `search.semantic_search`, `search.bfs_traversal`, `agents.coder_agent`, `core.llm_client`, `core.omop_client` |
| Referenced by other code? | **No** — only mentioned in `.md` documentation files (REFACTORING_REPORT.md, STATIC_ANALYSIS_REPORT.md, REPOSITORY_INVENTORY.md, CHANGELOG_V14.md) |
| **Safe to delete?** | **YES** — root-level debug script, superseded by `tests/debug/debug_cpt_engine.py` |

---

## 2. debug_dan_williams.py

| Field | Value |
|---|---|
| Purpose | Debug script to test physician agent extraction on the Dan Williams operative report |
| Imports from main codebase | `core.models`, `agents.physician_agent`, `core.llm_client`, `search.semantic_search`, `core.omop_client` |
| Referenced by other code? | **No** — only in `.md` docs |
| **Safe to delete?** | **YES** — case-specific debug, no production dependency |

---

## 3. debug_llm_raw.py

| Field | Value |
|---|---|
| Purpose | Prints raw LLM response for the physician agent prompt on a sample note |
| Imports from main codebase | `core.llm_client`, `knowledge.prompts`, `core.models` |
| Referenced by other code? | **No** — only in `.md` docs |
| **Safe to delete?** | **YES** — diagnostic script, not wired into any pipeline |

---

## 4. debug_office.py

| Field | Value |
|---|---|
| Purpose | Tests the full coding pipeline on an office-visit clinical note |
| Imports from main codebase | `core.models`, `search.semantic_search`, `search.bfs_traversal`, `agents.coder_agent`, `core.llm_client`, `core.omop_client` |
| Referenced by other code? | **No** — only in `.md` docs |
| **Safe to delete?** | **YES** — standalone test at root level |

---

## 5. debug_string_replace.py

| Field | Value |
|---|---|
| Purpose | Tests string `.replace("json","")` behavior on a sample LLM JSON response |
| Imports from main codebase | None (pure Python, no project imports) |
| Referenced by other code? | **No** — only in `.md` docs |
| **Safe to delete?** | **YES** — trivial one-off string test, zero project dependency |

---

## 6. debug_try_parse_json.py

| Field | Value |
|---|---|
| Purpose | Tests `LLMClient.try_parse_json` on a markdown-fenced JSON response |
| Imports from main codebase | `core.llm_client` |
| Referenced by other code? | **No** — only in `.md` docs |
| **Safe to delete?** | **YES** — diagnostic script |

---

## 7. test_clean.py

| Field | Value |
|---|---|
| Purpose | Tests `clean_search_query` function on colonoscopy-related terms |
| Imports from main codebase | `search.semantic_search.clean_search_query` |
| Referenced by other code? | **No** — only in `.md` docs. Note: `test_v16_enterprise.py` has methods named `test_clean_note_passes` and `test_clean_specific_note_no_queries` but those are unrelated — they test different functionality |
| **Safe to delete?** | **YES** — root-level ad-hoc test, proper tests exist in `tests/` |

---

## 8. test_cpt.py

| Field | Value |
|---|---|
| Purpose | Tests `SemanticSearch.search_direct` with CPT vocabulary on several procedure terms |
| Imports from main codebase | `core.omop_client`, `search.semantic_search` |
| Referenced by other code? | **No** — grep hits for `test_cpt` are all `test_cpt_reasoning.py`, `test_cpt_coding`, `test_cpt_family_lookup` — completely different files |
| **Safe to delete?** | **YES** — root-level ad-hoc test |

---

## 9. test_debug.py

| Field | Value |
|---|---|
| Purpose | Runs the full pipeline (physician → coder → BFS) on a hypertension/colonoscopy note |
| Imports from main codebase | `core.models`, `search.semantic_search`, `search.bfs_traversal`, `agents.coder_agent`, `core.llm_client`, `core.omop_client` |
| Referenced by other code? | **No** — only in `.md` docs |
| **Safe to delete?** | **YES** — root-level debug test |

---

## 10. test_influenza.py

| Field | Value |
|---|---|
| Purpose | Tests `clean_search_query` and CPT search for influenza vaccine variants |
| Imports from main codebase | `core.omop_client`, `search.semantic_search` |
| Referenced by other code? | **No** — only in `.md` docs |
| **Safe to delete?** | **YES** — ad-hoc CPT search test |

---

## 11. test_json_parsing.py

| Field | Value |
|---|---|
| Purpose | Tests `LLMClient.try_parse_json` on a markdown-fenced JSON with invalid expressions (e.g. `1.0 + (2 * 0.3)`) |
| Imports from main codebase | `core.llm_client` |
| Referenced by other code? | **No** — only in `.md` docs |
| **Safe to delete?** | **YES** — root-level diagnostic |

---

## 12. test_json_parsing_fixed.py

| Field | Value |
|---|---|
| Purpose | Variant of test_json_parsing.py with additional `json`/`re` imports and fixed parsing approach |
| Imports from main codebase | `core.llm_client` |
| Referenced by other code? | **No** — only in `.md` docs. STATIC_ANALYSIS_REPORT.md notes it's a duplicate of test_json_parsing.py |
| **Safe to delete?** | **YES** — duplicate diagnostic script |

---

## 13. test_llm_response.py

| Field | Value |
|---|---|
| Purpose | Sends the Dan Williams note to LLM and prints the raw extraction response |
| Imports from main codebase | `core.llm_client`, `knowledge.prompts.PHYSICIAN_AGENT_PROMPT` |
| Referenced by other code? | **No** — only in `.md` docs |
| **Safe to delete?** | **YES** — ad-hoc LLM test |

---

## 14. test_ml_keloid.py

| Field | Value |
|---|---|
| Purpose | Tests `ClinicalNLPExtractor` for keloid lesion extraction, lesion counting, and encounter classification |
| Imports from main codebase | `ml.clinical_nlp` (ClinicalNLPExtractor, extract_lesion_count, classify_encounter_category) |
| Referenced by other code? | **No** — only self-references and `.md` docs |
| **Safe to delete?** | **YES** — standalone ML test for a specific clinical scenario, not imported anywhere |

---

## 15. test_omop.py

| Field | Value |
|---|---|
| Purpose | Tests `OMOPClient.semantic_search` for colonoscopy-related CPT terms |
| Imports from main codebase | `core.omop_client` |
| Referenced by other code? | **No** — only in `.md` docs |
| **Safe to delete?** | **YES** — root-level OMOP test |

---

## 16. test_v16_e2e.py

| Field | Value |
|---|---|
| Purpose | End-to-end test: starts uvicorn server, sends clinical note to `/api/v16/code`, validates response schema |
| Imports from main codebase | None at top-level (uses `urllib.request` against running server) |
| Referenced by other code? | **No** — only in `.md` docs |
| Safe to delete? | **YES** — root-level e2e script. The `tests/` directory likely contains proper e2e equivalents. Could consider moving to `tests/` but has no code dependency |

---

## 17. final_test_fixed.py

| Field | Value |
|---|---|
| Purpose | Runs the orchestrator on the Dan Williams note and prints coding results |
| Imports from main codebase | `core.models`, `agents.orchestrator`, `core.llm_client`, `core.omop_client` |
| Referenced by other code? | **No** — only in `.md` docs. STATIC_ANALYSIS_REPORT.md calls it a duplicate of `final_test.py` |
| **Safe to delete?** | **YES** — duplicate test script |

---

## 18. full_pipeline_test.py

| Field | Value |
|---|---|
| Purpose | Runs the full multi-agent pipeline (physician → patient → reviewer → adjuster → coder → orchestrator) on a procedure note |
| Imports from main codebase | `core.models`, `search.semantic_search`, `search.bfs_traversal`, `agents.coder_agent`, `agents.physician_agent`, `agents.patient_agent`, `agents.reviewer_agent`, `agents.adjuster_agent`, `agents.orchestrator`, `core.llm_client`, `core.omop_client` |
| Referenced by other code? | **No** — only in `.md` docs |
| **Safe to delete?** | **YES** — comprehensive but root-level integration test with no production dependency |

---

## 19. preprocess_mimic.py

| Field | Value |
|---|---|
| Purpose | One-time preprocessing script for MIMIC-III/IV discharge summaries → Parquet files |
| Imports from main codebase | **None** — fully standalone (uses `argparse`, `csv`, `gzip`, `json`, `os`, `re`, `sys`, `pathlib`, `pandas`, `tqdm`) |
| Referenced by other code? | **No** — only in `.md` docs |
| **Safe to delete?** | **YES** — self-contained data prep utility with no project imports. However, consider moving to `scripts/` if MIMIC evaluation is still planned |

---

## 20. llm_output.txt

| Field | Value |
|---|---|
| Purpose | Contains a JSON dump of a physician agent extraction (Dan Williams operative report) |
| Imports from main codebase | N/A (text data file) |
| Referenced by other code? | **No** — only in `.md` docs |
| **Safe to delete?** | **YES** — debug output artifact |

---

## 21. test_file.txt

| Field | Value |
|---|---|
| Purpose | Contains two clinical case studies (Dan Williams excision + Bill observation encounter) as raw text |
| Imports from main codebase | N/A (text data file) |
| Referenced by other code? | **No** — only in `.md` docs |
| **Safe to delete?** | **YES** — test data artifact |

---

## 22. case_list.txt

| Field | Value |
|---|---|
| Purpose | Lists ~50+ test case titles (e.g. "Case 1: Outpatient Hypertension Follow-up") |
| Imports from main codebase | N/A (text data file) |
| Referenced by other code? | **No** — only in STATIC_ANALYSIS_REPORT.md |
| Safe to delete? | **YES** — could consider preserving as documentation of test case taxonomy, but it has no code dependency |

---

## 23. _generate_init.py

| Field | Value |
|---|---|
| Purpose | One-time utility that generates `__init__.py` files for `evidence_engine/` and `compliance/` packages |
| Imports from main codebase | None (uses only `os`) |
| Referenced by other code? | **No** — only in STATIC_ANALYSIS_REPORT.md |
| **Safe to delete?** | **YES** — code-generation script whose output (`__init__.py` files) already exist. The script itself has no ongoing role |

---

## 24. nul

| Field | Value |
|---|---|
| Purpose | Accidental file — contains a bash error message (`/usr/bin/bash: line 1: del: command not found`) |
| Imports from main codebase | N/A |
| Referenced by other code? | **No** — only in `.md` docs. The `icd_book_data.json` grep hit is a false positive (the string "nul" appears inside an ICD description) |
| **Safe to delete?** | **YES** — Windows artifact from a failed `del` command |

---

## Summary

| Verdict | Count | Files |
|---|---|---|
| **SAFE TO DELETE** | **24** | All files |

Every file in this list is either:
1. A **root-level debug/test script** with no imports from production code (consumers only)
2. A **data/output artifact** (`.txt` files, `nul`)
3. A **one-time utility** whose output already exists (`_generate_init.py`)

**No file imports or is imported by production source code** (`core/`, `agents/`, `search/`, `ml/`, `compliance/`, `evidence_engine/`). All grep hits for these filenames are in `.md` documentation only.

### Recommendation

All 24 files are safe to delete. If you want to preserve `preprocess_mimic.py` for future MIMIC benchmarking, move it to `scripts/preprocess_mimic.py` instead of deleting.
